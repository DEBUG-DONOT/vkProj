# 1. 默认忽略当前目录下的所有文件和文件夹
*

# 2. 【例外】不忽略文件夹结构（必须保留，否则无法扫描子目录）
!*/

# 3. 【例外】白名单：放行 src 目录及其下的所有内容
!src/
!src/**

# 4. 【例外】白名单：放行指定的文件
!CMakeLists.txt
!README.md

# 5. 【例外】建议同时放行 .gitignore 本身，方便版本控制
!.gitignore